/**
 * Tailwind CSS v4 Configuration
 *
 * NOTE: Tailwind CSS v4 no longer uses this JS config file for PostCSS builds.
 * All theme customizations (colors, fonts, keyframes, etc.) are defined directly
 * inside src/index.css using the @theme block.
 *
 * This file is kept for IDE intellisense support only.
 */
export default {}
